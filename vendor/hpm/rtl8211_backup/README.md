# RTL8211 Backup

This directory keeps an older backup copy of RTL8211 driver sources for reference and diffing.

It is not used by the current build. The active RTL8211 driver now comes from `Clight/vendor/enet_phy/rtl8211/`.
